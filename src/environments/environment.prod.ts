// Non-secret production defaults. For local builds, secrets still come from config.local.json.
export const environment = {
  production: true,
  api_baseurl: 'https://letgetstarted-server.herokuapp.com/',
};